@model IEnumerable<StreamFaction.Models.Users>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.usr_login)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.usr_psw)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.usr_create_date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.usr_modif_date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.usr_login)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.usr_psw)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.usr_create_date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.usr_modif_date)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.usr_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.usr_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.usr_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
